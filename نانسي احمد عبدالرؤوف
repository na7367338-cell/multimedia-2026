# Task 4: remove_from_list
def remove_from_list():
    # تعريف دالة اسمها remove_from_list
    my_list = ['Apple', 'Banana', 'Cherry', 'Data']
    # إنشاء قائمة تحتوي على 4 عناصر نصية
    item = input("Enter the item you want to remove: ")
    # مطالبة المستخدم بإدخال العنصر الذي يريد حذفه
    if item in my_list:
        # التحقق مما إذا كان العنصر موجودًا في القائمة
        my_list.remove(item)
        # إزالة العنصر من القائمة
        print(f"{item} has been removed successfully.")
        # طباعة رسالة تؤكد أن العنصر تم حذفه بنجاح
    else:
        # إذا لم يكن العنصر موجودًا في القائمة
        print(f"{item} was not found in the list.")
        # طباعة رسالة تفيد أن العنصر غير موجود
    print("Updated list:", my_list)
    # طباعة القائمة بعد التحدي

# Task 5: calculate
def calculate_factorial():
    # اتعريف دالة اسمه calculate_factorial
    n = int(input("Enter a positive integer: "))
    # مطالبة المستخدم بإدخال عدد صحيح موجب
    result = 1
    # تهيئة متغير باسم result بقيمة 1 لتخزين الناتج
    for i in range(1, n + 1):
        # استخدام حلقة for للتكرار من 1 إلى n (بما في ذلك n)
        result = result * i
        # ضرب النتيجة الحالية في رقم الحلقة الحالي (i)
    print(f"The factorial of {n} is: {result}")
    # طباعة الناتج النهائي لمضروب العدد
Main Program Execution
print("Choose a task to run:")
print("1 - Remove an item from the list (Task 4)")
print("2 - Calculate factorial of a number (Task 5)")
# عرض قائمة للمستخدم لاختيار أي مهمة يريد تشغيلها
choice = input("Enter your choice (1 or 2): ")
# مطالبة المستخدم باختيار رقم المهمة
if choice == "1":
    # إذا اختار المستخدم المهمة رقم 1
    remove_from_list()
    # استدعاء دالة إزالة العنصر من القائمة
elif choice == "2":
    # إذا اختار المستخدم المهمة رقم 2
    calculate_factorial()
    # استدعاء دالة حساب المضروب
else:
    # في حال أدخل المستخدم اختيارًا غير صحيح
    print("Invalid choice. Please enter 1 or 2.")
    # طباعة رسالة خطأ
