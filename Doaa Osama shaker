Task 4
  def remove_from_list():
    # عرفنا ليست فيها أربع عناصر
    my_list = ['Apple', 'Banana', 'Cherry', 'Data']

    # نطلب من المستخدم يدخل العنصر اللي عايز يشيله
    item = input("Enter the item you want to remove: ").strip()

    # نتحقق إذا العنصر موجود في الليست
    if item in my_list:
        # لو موجود، نحذفه
        my_list.remove(item)
        # نطبع رسالة نجاح
        print(f"'{item}' has been removed from the list.")
    else:
        # لو مش موجود، نطبع رسالة إنه غير موجود
        print(f"'{item}' was not found in the list.")

    # نعرض الليست بعد التعديل
    print("Updated list:", my_list)


# نستدعي الدالة علشان تشتغل
remove_from_list()
      Task 5
      def calculate_factorial():
    # طلب إدخال رقم صحيح موجب من المستخدم
    n = int(input("Enter a positive integer: "))

    # تهيئة المتغير result بقيمة 1 (لأن 1! = 1)
    result = 1

    # حلقة for لضرب الأرقام من 1 إلى n
    for i in range(1, n + 1):
        result = result * i  # ضرب النتيجة الحالية في i

    # طباعة النتيجة النهائية
    print("The factorial of", n, "is", result)
